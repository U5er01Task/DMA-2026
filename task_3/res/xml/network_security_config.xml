<?xml version="1.0" encoding="utf-8"?>
<!--
    Тема 2, задание 2. Конфиг сетевой безопасности с SSL-Pinning.
    - HTTP (cleartext) запрещен везде
    - доверяем только системным сертификатам (пользовательские CA не принимаем)
    - для api.bank.example закреплены хэши публичных ключей (основной + запасной)
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.bank.example</domain>
        <pin-set expiration="2027-12-31">
            <!-- SHA-256 от SubjectPublicKeyInfo сертификата сервера -->
            <pin digest="SHA-256">7HIpactkIAq2Y49orFOOQKurWxmmSFZhBCoQYcRhJ3Y=</pin>
            <!-- запасной ключ, чтобы приложение не сломалось при смене сертификата -->
            <pin digest="SHA-256">fwza0LRMXouZHRC8Ei+4PyuldPDcf3UKgO/04cDM1oE=</pin>
        </pin-set>
    </domain-config>

    <!-- В debug-сборке разрешаем пользовательские сертификаты (для Charles/mitmproxy) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
