<?xml version="1.0" encoding="utf-8"?>
<!--
    Тема 3, задание 2. Изоляция служебного сервиса

    exported="false" и нет intent-filter - сервис можно запустить только из нашего приложения
    (явным Intent). Другие приложения получат SecurityException.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ru.kulikov.pr3">

    <application android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.App">

        <service
            android:name=".data.DatabaseSyncService"
            android:exported="false"
            android:enabled="true" />

    </application>

</manifest>
