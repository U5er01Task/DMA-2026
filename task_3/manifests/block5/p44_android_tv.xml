<?xml version="1.0" encoding="utf-8"?>
<!--
    Практика 44. Android TV

    LEANBACK_LAUNCHER - иконка в лаунчере телевизора (обычный LAUNCHER тоже оставлен для телефонов).
    touchscreen required="false" - иначе Google Play не покажет приложение на ТВ.
    banner 320x180 - обязательная картинка для домашнего экрана Android TV.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ru.kulikov.pr3">

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.App"
        android:banner="@drawable/tv_banner">

        <activity
            android:name=".tv.TvMainActivity"
            android:exported="true"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
