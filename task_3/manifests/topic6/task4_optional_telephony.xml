<?xml version="1.0" encoding="utf-8"?>
<!--
    Тема 6, задание 4. Телефония и SMS необязательны (планшеты)

    Разрешения CALL_PHONE и SEND_SMS неявно делают android.hardware.telephony обязательной.
    Явно указываем required="false", чтобы приложение ставилось на планшеты без SIM.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ru.kulikov.pr3">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <application android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.App" />

</manifest>
